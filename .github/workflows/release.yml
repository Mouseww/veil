name: release
on:
  push:
    tags: ["v*"]
permissions:
  contents: write
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            asset: veil-windows-x64.exe
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            asset: veil-linux-x64
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            asset: veil-linux-arm64
          - os: macos-latest
            target: aarch64-apple-darwin
            asset: veil-macos-arm64
          - os: macos-latest
            target: x86_64-apple-darwin
            asset: veil-macos-x64
    runs-on: ${{ matrix.os }}
    env:
      VEIL_SKIP_UI_BUILD: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install aarch64 linker
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: sudo apt-get update && sudo apt-get install -y gcc-aarch64-linux-gnu
      - name: build ui
        working-directory: ui
        run: npm ci && npm run build
      - name: build binary
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
          CC_aarch64_unknown_linux_gnu: aarch64-linux-gnu-gcc
        run: cargo build --release -p veil --target ${{ matrix.target }}
      - name: package
        shell: bash
        run: |
          mkdir -p dist
          bin="target/${{ matrix.target }}/release/veil"
          if [ -f "${bin}.exe" ]; then bin="${bin}.exe"; fi
          cp "$bin" "dist/${{ matrix.asset }}"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: dist/${{ matrix.asset }}
  checksums:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: SHA256SUMS
        working-directory: dist
        run: sha256sum * > SHA256SUMS
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
